mod ds;
mod ex;

use crate::ds::list_node::{array_to_list, ListNode};
use crate::ex::*;

fn main() {
    println!(
        "{:?}",
        ex::_67::add_binary(String::from("1010"), String::from("1011"))
    );
}
